#include "main.h"
/**
 * _sqrt_recursion - return the square root
 * @n: number
 * Return: the square root
 */
int _sqrt_recursion(int n)
{
	int a = n ** (1/2);

	if (a * a = n)
		return (a);
	return (-1);
}
