#include<stdio.h>
/**
 * the main funcation prints the pharse "Programming is like building a multilingual puzzle
 * return 0 if done ,non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
