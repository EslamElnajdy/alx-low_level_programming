#include <stdio.h>
/**
 * main - print the file name
 * @argc: the number of command line arguments
 * @argv: the array of string from command line arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv[argc * 0]);
	return (0);
}
